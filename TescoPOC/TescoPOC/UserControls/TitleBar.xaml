<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
			xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"

			x:Class="TescoPOC.TitleBar" >
	<ContentView.Content>
	 	<StackLayout VerticalOptions="Center" HorizontalOptions="CenterAndExpand" x:Name="TitleBar" >
	        <Image BackgroundColor="Fuchsia" Height="20dp" Width="20dp" HorizontalOptions="Start" />
		    <Label HorizontalOptions="CenterAndExpand"
		           x:Name="TitleLabel" />
		    <Image BackgroundColor="Fuchsia" Height="20dp" Width="20dp" HorizontalOptions="End" />
		</StackLayout>
	</ContentView.Content>
</ContentView>
